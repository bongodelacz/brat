# BratClient — frontend (.env)
# Skopiuj do frontend/.env. Na Vercelu ustaw tę zmienną w Project Settings -> Environment Variables.

# URL backendu (Render/Railway/Docker). BEZ /api i BEZ / na końcu — kod sam dokleja /api.
REACT_APP_BACKEND_URL=http://localhost:8001
